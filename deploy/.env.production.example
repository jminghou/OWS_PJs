# =============================================================================
# Polaris Parent - NAS Production 環境變數範本
# =============================================================================
# 使用說明：
# 1. 複製此檔案為 .env.production：
#    cp .env.production.example .env.production
# 2. 填入下方「必填/需檢查」區塊中的實際數值。
# 3. 產生安全金鑰 (用於 SECRET_KEY)：
#    python -c "import secrets; print(secrets.token_hex(32))"
# =============================================================================

# -----------------------------------------------------------------------------
# 🚩 第一部分：必填與核心設定 (部署前請務必檢查並修改)
# -----------------------------------------------------------------------------

# [資料庫設定]
DB_USER=polaris
DB_PASSWORD=Polaris_NAS_Jermaine
DB_NAME=polaris_db

# [安全金鑰] - 請務必更換，不要使用預設值
# 建議使用上述 python 指令產生隨機字串
SECRET_KEY=2dfe5124986cc8277f061b2912ab041b7db90f2f899655b21a03ba02d44fa5f0
JWT_SECRET_KEY=95e4f161a00d2ae8c246a29aa57717962d326f4a3fe0a2602c7106361a689db1

# [網域與 CORS 設定]
# CORS_ORIGINS: 允許存取 API 的前端網址，多個網址請用逗號隔開
CORS_ORIGINS=https://frontend.polaris-parent.com,http://192.168.30.65:3000

# [前端 URL 設定] - 這些會被編譯進 Next.js 前端
# NEXT_PUBLIC_API_URL: 前端呼叫 API 的路徑 (通常透過 Next.js Proxy)
NEXT_PUBLIC_API_URL=https://frontend.polaris-parent.com/api/v1
# NEXT_PUBLIC_BACKEND_URL: 後端 API 的實際公開網址 (用於圖片路徑等)
NEXT_PUBLIC_BACKEND_URL=https://api.polaris-parent.com
# NEXT_PUBLIC_SITE_URL: 前端網站的公開網址
NEXT_PUBLIC_SITE_URL=https://frontend.polaris-parent.com

# [Google Cloud Storage (GCS)]
GCS_BUCKET_NAME=polarisparent_pic
# GCS 憑證檔案在容器內的路徑
GCS_CREDENTIALS_JSON=/app/secrets/gcs.json


# -----------------------------------------------------------------------------
# ⚙️ 第二部分：進階與系統預設值 (通常維持預設即可)
# -----------------------------------------------------------------------------

# [Flask 運行環境]
FLASK_CONFIG=production

# [JWT Cookie 設定]
# SameSite=None 允許跨子網域 (如 frontend. 與 api.) 傳送 Cookie
JWT_COOKIE_SAMESITE=None
JWT_ACCESS_TOKEN_EXPIRES_HOURS=24

# [站點資訊]
NEXT_PUBLIC_SITE_NAME=Polaris Parent
SITE_NAME=Polaris Parent
DEFAULT_LANGUAGE=zh-TW
SUPPORTED_LANGUAGES=zh-TW,en

# [效能設定]
# Gunicorn 工作進程數，建議設為 (2 x CPU核心數) + 1
GUNICORN_WORKERS=2