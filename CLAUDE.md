# CLAUDE.md — OWS

## 程式導航：先查 graphify 知識圖譜，再讀檔

本 repo 有一張 **code-only 程式知識圖譜**，透過 MCP server **`graphify-ows`** 提供唯讀查圖工具。
遇到「**結構 / 導航 / 影響分析**」類問題時，**先查圖、拿到帶 `file:line` 的節點，再去讀那幾個檔**——不要一開始就 grep 或整包翻檔。

**該先查圖的情境（對應工具）**
- 「X 在哪定義、被誰呼叫」→ `get_node` / `get_neighbors`
- 「改動 X 會波及誰」（反向影響）→ `affected`
- 「A 到 B 怎麼連起來」→ `shortest_path`
- 「某主題涉及哪些程式 / 廣泛定位」→ `query_graph`
- 「整個 repo 的架構樞紐、有哪些子系統」→ `god_nodes` / `graph_stats`

**流程**：查圖 → 讀回傳節點的 `file:line` → 只讀那幾個檔做確認或修改。圖回的是「帶引用的節點集合」不是答案，推理與讀懂細節仍是你的事。

**不要用這張圖的情境**
- 「這段程式在做什麼」的語意理解 → 直接讀該檔。
- 搜資料庫**資料內容** → 走應用查詢層或 SQL，不是這張圖。
- 三五個檔案的小改動 → 直接做，別繞。

圖是 code-only，每次 commit 由 git hook 自動增量更新，不需手動維護。
